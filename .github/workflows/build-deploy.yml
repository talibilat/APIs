name: Build and Deploy Code

on: 
  push: 
    branches: ['main'] 
  pull_request:
    branches: ['main']

jobs:
  job1:
    environment:
      name: Test
    env:
      DATABASE_HOSTNAME: ${{secrets.DATABASE_HOSTNAME}}
      DATABASE_PORT: ${{secrets.DATABASE_PORT}} 
      DATABASE_PASSWORD: ${{secrets.DATABASE_PASSWORD}}
      DATABASE_NAME: ${{secrets.DATABASE_NAME}}
      DATABASE_USERNAME: ${{secrets.DATABASE_USERNAME}}
      SECRET_KEY: ${{secrets.SECRET_KEY}}
      ALGORITHM: ${{secrets.ALGORITHM}}  
      ACCESS_TOKEN_EXPIRE_MINUTES: ${{secrets.ACCESS_TOKEN_EXPIRE_MINUTES}}  

    services:
      postgres:
        image: postgres:latest
        env:
          POSTGRES_USER: ${{secrets.DATABASE_USERNAME}}       # Set your Postgres username
          POSTGRES_PASSWORD: ${{secrets.POSTGRES_PASSWORD}}    # Superuser password
          POSTGRES_DB: ${{secrets.DATABASE_NAME}}              # Default database name
        ports:
          - 5432:5432
        options: > 
          --health-cmd="pg_isready" 
          --health-interval=10s 
          --health-timeout=5s 
          --health-retries=5


    runs-on: ubuntu-latest

    
    steps:
      - name: pulling git repo
        uses: actions/checkout@v4  # Updated to the latest version (v4)

      - name: Install Python
        uses: actions/setup-python@v4  # Corrected to v4
        with:
          python-version: "3.10"

      - name: Update pip
        run: python -m pip install --upgrade pip

      - name: Install Dependencies 
        run: pip install -r requirements.txt

      - name: Run Tests
        run: |
          pip install pytest
          pytest
